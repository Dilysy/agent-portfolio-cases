# AutoFlow 四模式测试记录

## 测试前提

- 前端地址：`http://127.0.0.1:5173/`
- 后端健康检查：`/health` 可用
- 本次不读取、不记录、不输出真实 API Key。
- 当前真实 LLM 参数由用户在源项目后端本地 `.env` 中配置。
- 已新增规则优先的快速路径：标准模式、灵感模式会先尝试规则生成，规则命中时不调用 LLM。
- 判定标准：通过 Mermaid 结构校验并成功渲染，才标记为可演示。

## 总览

| 模式 | 是否成功 | 是否调用 LLM | 生成来源 | 是否生成 Mermaid | Mermaid 是否通过结构校验 | 是否成功渲染流程图 | 是否可用于演示 | 响应耗时 | 失败原因 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 灵感模式 | 成功 | 否 | rule-linear-plan | 是 | 是 | 是 | 是 | 0.087 秒 | - |
| 标准模式 | 成功 | 否 | rule-sales-process | 是 | 是 | 是 | 是 | 0.007 秒 | - |
| 计划模式 | 成功 | 否 | rule-linear-plan | 是 | 是 | 是 | 是 | 0.024 秒 | - |
| Mermaid 代码模式 | 成功 | 否 | manual-code | 是 | 是 | 是 | 是 | 0.000 秒 | - |

## 详细记录

### 1. 灵感模式

- 输入：我想做一个销售跟进流程，包含客户初次咨询、需求确认、方案编写、客户评审、合同签署几个阶段。
- 前端入口：灵感模式聊天输入
- 后端接口：POST /api/agent/chat/stream
- 是否调用 LLM：否
- 生成来源：rule-linear-plan
- 是否生成 Mermaid：是
- Mermaid 是否通过结构校验：是
- 结构指标：节点 6 个，连接 4 条，判断节点 无
- 是否成功渲染流程图：是
- 是否可用于演示：是
- 响应耗时：0.087 秒
- 结果：成功
- Mermaid 文件：`01-inspiration-mode-mermaid.md`

### 2. 标准模式

- 输入：客户提交需求后，销售进行需求确认。需求明确则进入方案编写，需求不明确则返回客户补充信息。方案完成后提交客户评审，客户通过后进入合同流程，未通过则返回修改。
- 前端入口：标准模式聊天输入
- 后端接口：POST /api/agent/chat/stream
- 是否调用 LLM：否
- 生成来源：rule-sales-process
- 是否生成 Mermaid：是
- Mermaid 是否通过结构校验：是
- 结构指标：节点 12 个，连接 13 条，判断节点 有
- 是否成功渲染流程图：是
- 是否可用于演示：是
- 响应耗时：0.007 秒
- 结果：成功
- Mermaid 文件：`02-standard-mode-mermaid.md`

### 3. 计划模式

- 输入：请为企业客户项目售前流程生成计划流程图，包含需求收集、方案设计、内部评审、客户评审、合同流程和后续交付启动。
- 前端入口：计划模式编辑器
- 后端接口：POST /api/plan
- 是否调用 LLM：否
- 生成来源：rule-linear-plan
- 是否生成 Mermaid：是
- Mermaid 是否通过结构校验：是
- 结构指标：节点 8 个，连接 7 条，判断节点 无
- 是否成功渲染流程图：是
- 是否可用于演示：是
- 响应耗时：0.024 秒
- 结果：成功
- Mermaid 文件：`03-plan-mode-mermaid.md`

### 4. Mermaid 代码模式

- 输入：flowchart TD
    A[客户提交需求] --> B[销售进行需求确认]
    B --> C{需求是否明确?}
    C -- 是 --> D[进入方案编写阶段]
    C -- 否 --> E[返回客户补充信息]
    E --> B
    D --> F[方案完成]
    F --> G[提交客户评审]
    G --> H{客户是否通过?}
    H -- 是 --> I[进入合同流程]
    H -- 否 --> J[返回修改]
    J --> D
- 前端入口：Mermaid 代码模式编辑器
- 后端接口：无后端生成接口
- 是否调用 LLM：否
- 生成来源：manual-code
- 是否生成 Mermaid：是
- Mermaid 是否通过结构校验：是
- 结构指标：节点 10 个，连接 11 条，判断节点 有
- 是否成功渲染流程图：是
- 是否可用于演示：是
- 响应耗时：0.000 秒
- 结果：成功
- Mermaid 文件：`04-code-mode-mermaid.md`
